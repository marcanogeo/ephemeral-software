---
import Layout from '../layouts/Layout.astro';
import DynamicBanner from '../components/DynamicBanner.astro';
import { backgroundAssets } from '../assets';

const contactFormUrl = "/contactform";
---

<Layout pageTitle="Contact - Ephemeral" backgroundType="contact">
    <DynamicBanner 
        pageType="contact"
        title="Contact Us"
        description="Ready to start your next project? Get in touch with our team of experts and let's build something amazing together."
    />
    
    <section class="bg-gray-50 py-20">
      <div class="max-w-[1440px] mx-auto px-6 text-center">
        <h1 class="text-3xl md:text-4xl max-w-4xl mx-auto font-bold text-gray-800 mb-6 pb-6">
          Your strategic technology partner for transforming ideas into real results
        </h1>

        <p class="text-lg md:text-xl text-gray-600 max-w-3xl mx-auto mb-6">
          At <span class="ephemeral-brand">Ephemeral Software</span> we believe every project deserves more than a vendor: it deserves a 
          <span class="font-semibold ephemeral-brand">strategic partner</span>.  
          We work with you from day one to understand your challenges, co-create innovative solutions, 
          and deliver them with the speed your business needs.
        </p>

        <p class="text-lg text-gray-600 max-w-2xl mx-auto mb-12">
          Our commitment: <span class="commitment-highlight">response within 24 hours</span> 
          and a clear plan to transform your ideas into competitive advantages.
        </p>

        <div class="grid md:grid-cols-3 gap-8 max-w-4xl mx-auto mb-12">
          <div class="flex-col bg-white shadow-md rounded-xl p-6">
            <h3 class="text-xl font-semibold text-gray-800 mb-2">⚡ <br> Speed & Commitment</h3>
            <p class="text-gray-600">You'll always get a response within 24 hours.</p>
          </div>
          <div class="flex-col bg-white shadow-md rounded-xl p-6">
            <h3 class="text-xl font-semibold text-gray-800 mb-2">🤝 <br> Real Co-creation</h3>
            <p class="text-gray-600">We design solutions with you, not for you.</p>
          </div>
          <div class="flex-col bg-white shadow-md rounded-xl p-6">
            <h3 class="text-xl font-semibold text-gray-800 mb-2">🚀 <br> Digital Transformation</h3>
            <p class="text-gray-600">From strategy to implementation, we support you through the entire process.</p>
          </div>
        </div>

        <a
          href={contactFormUrl}
          class="inline-block bg-blue-600 text-white font-medium px-8 py-3 rounded-full shadow-lg hover:bg-blue-700 transition"
          aria-label="Schedule consultation - Go to contact form"
        >
          Schedule My Consultation
        </a>
      </div>
    </section>
</Layout>

<style>
    /* === BLUE TEXT STYLES === */
    .ephemeral-brand {
        color: #2563eb !important; /* ✅ Force text-blue-600 */
        font-weight: 600;
    }
    
    .commitment-highlight {
        color: #2563eb !important; /* ✅ Force text-blue-600 */
        font-weight: 600;
    }
    
    /* === ENSURE BLUE OVERRIDE === */
    .text-gray-600 .ephemeral-brand,
    .text-gray-600 .commitment-highlight {
        color: #2563eb !important;
    }
    
    /* === SMOOTH TRANSITION === */
    section {
        animation: slideInFromBottom 0.6s ease-out 0.3s both;
    }
    
    @keyframes slideInFromBottom {
        from {
            opacity: 0;
            transform: translateY(30px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }
</style>