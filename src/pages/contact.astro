---
import Layout from '../layouts/Layout.astro';
import DynamicBanner from '../components/DynamicBanner.astro';
import inprogrees from '../assets/in-progress.svg';
import { backgroundAssets } from '../assets';

// Preload de la imagen del banner para evitar flash
const contactBg = backgroundAssets.contact;
---

<Layout pageTitle="Contact - Ephemeral" backgroundType="contact">
    <!-- Preload crítico del fondo de banner -->
    <link rel="preload" as="image" href={contactBg.src} />
    
    <DynamicBanner 
        pageType="contact"
        title="Contact Us"
        description="Ready to start your next project? Get in touch with our team of experts and let's build something amazing together."
    />
    
<section class="bg-gray-50 py-20">
  <div class="max-w-[1440px] mx-auto px-6 text-center">
    <h1 class="text-3xl md:text-4xl max-w-4xl mx-auto font-bold text-gray-800 mb-6 pb-6">
      Tu aliado tecnológico para transformar ideas en resultados reales
    </h1>

    <p class="text-lg md:text-xl text-gray-600 max-w-3xl mx-auto mb-6">
      En <span class="font-semibold text-blue-600">Ephemeral Software</span> creemos que cada proyecto merece más que un proveedor: merece un 
      <span class="font-semibold">socio estratégico</span>.  
      Trabajamos contigo desde el primer día para entender tus retos, co-crear soluciones innovadoras 
      y entregarlas con la rapidez que tu negocio necesita.
    </p>

    <p class="text-lg text-gray-600 max-w-2xl mx-auto mb-12">
      Nuestro compromiso: <span class="font-semibold text-blue-600">respuesta en menos de 24 horas</span> 
      y un plan claro para transformar tus ideas en ventajas competitivas.
    </p>

    <div class="grid md:grid-cols-3 gap-8 max-w-4xl mx-auto mb-12">
      <div class="flex-col bg-white shadow-md rounded-xl p-6">
        <h3 class="text-xl font-semibold text-gray-800 mb-2">⚡ <br> Rapidez y compromiso</h3>
        <p class="text-gray-600">Siempre tendrás una respuesta en menos de 24 horas.</p>
      </div>
      <div class="flex-col bg-white shadow-md rounded-xl p-6">
        <h3 class="text-xl font-semibold text-gray-800 mb-2">🤝 <br> Co-creación real</h3>
        <p class="text-gray-600">Diseñamos soluciones junto a ti, no para ti.</p>
      </div>
      <div class="flex-col bg-white shadow-md rounded-xl p-6">
        <h3 class="text-xl font-semibold text-gray-800 mb-2">🚀 <br> Transformación tecnológica</h3>
        <p class="text-gray-600">De la estrategia a la implementación, te acompañamos en todo el proceso.</p>
      </div>
    </div>

    <a
      href="/contact-form"
      class="inline-block bg-blue-600 text-white font-medium px-8 py-3 rounded-full shadow-lg hover:bg-blue-700 transition"
    >
      Agendar mi consulta
    </a>
  </div>
</section>

</Layout>

<style>
    /* Optimizaciones específicas para transiciones suaves en contact */
    .dynamic-banner {
        min-height: 100vh;
        background: #04335C;
    }
    
    /* Transición suave para la sección de contacto */
    #contact-form {
        animation: slideInFromBottom 0.6s ease-out 0.5s both;
    }
    
    @keyframes slideInFromBottom {
        from {
            opacity: 0;
            transform: translateY(30px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }
</style>